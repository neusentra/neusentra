export const ENDPOINTS = {
    // Initialization
    initializeStatus: '/setup/status',
    initialize: '/setup/initialize',

    // Auth
    login: '/auth/login',
    logout: '/auth/logout',

    // SSE
    sseEvents: '/v1/events/server',
}